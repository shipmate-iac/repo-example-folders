name: apply
on:
  workflow_dispatch:
    inputs:
      environment:
        description: Target environment (empty = bare `mate apply`, all non-explicit environments)
        required: false
        default: ''
      ref: { description: PR head SHA to apply, required: true }
      pr_number: { description: PR number, required: true }
      plan_run_id: { description: Preview run id with the reviewed plans, required: true }
permissions:
  contents: read
  checks: write
  actions: read
  pull-requests: read
jobs:
  targeted:
    if: ${{ inputs.environment != '' }}
    uses: ship-iac/shipmate/.github/workflows/apply.yml@6adc944eec3c80e9dd64d33a6a2070c08b1b2d29
    permissions: { contents: read, checks: write, actions: read }
    secrets: inherit
    with:
      environment: ${{ inputs.environment }}
      ref: ${{ inputs.ref }}
      pr_number: ${{ inputs.pr_number }}
      plan_run_id: ${{ inputs.plan_run_id }}
      state_suffix: terraform.tfstate
  all:
    if: ${{ inputs.environment == '' }}
    uses: ship-iac/shipmate/.github/workflows/apply-all.yml@6adc944eec3c80e9dd64d33a6a2070c08b1b2d29
    permissions: { contents: read, checks: write, actions: read }
    secrets: inherit
    with:
      ref: ${{ inputs.ref }}
      pr_number: ${{ inputs.pr_number }}
      plan_run_id: ${{ inputs.plan_run_id }}
      state_suffix: terraform.tfstate
